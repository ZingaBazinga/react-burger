import { createSlice } from "@reduxjs/toolkit";

const ingredientDetailsSlice = createSlice({
    name: "ingredientDetails",
    initialState: {},
    reducers: {},
});

export const {} = ingredientDetailsSlice.actions;
export default ingredientDetailsSlice.reducer;
